Now;
	- Store Prev/Next pointers separately.

Later:
	- Ability to store fixed-size 8 byte int63s, variable-sized in-place data and variable-sized blob data.
	- Support batching.
	- Freelist.

Before release:
	- Better error messages.
	- Snapshots.

	- Bloom filter for Has().
	- LRU cache for reads.


- Benchmark against in-memory, FS-based and 3rd-party.